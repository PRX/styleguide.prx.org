import { OnChanges, ElementRef } from '@angular/core';
export declare class StackedBarCategoryRotatedChartComponent implements OnChanges {
    groups: string[];
    categories: string[];
    data: number[][];
    percent: boolean;
    chart: any;
    el: ElementRef;
    constructor(elementRef: ElementRef);
    ngOnChanges(): void;
}
