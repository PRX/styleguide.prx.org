import { EventEmitter, OnChanges } from '@angular/core';
import { IndexedChartModel, LegendItemModel } from '../models';
export declare class PieChartComponent implements OnChanges {
    datasets: IndexedChartModel[];
    secondaryDatasets: IndexedChartModel[];
    onLabelClick: EventEmitter<{}>;
    chart: any;
    private chartElement;
    columnData: any[][];
    colors: string[];
    legendItems: LegendItemModel[];
    secondaryItems: LegendItemModel[];
    ngOnChanges(): void;
    focusChartData(dataId: string): void;
}
